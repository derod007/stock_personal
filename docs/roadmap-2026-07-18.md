# noramu 로드맵

| 항목 | 내용 |
|------|------|
| 작성일 | 2026-07-18 |
| 기준 문서 | `README.md`, `docs/account1-playbook.md`, `docs/retrospective-entries.md` |
| 목적 | 현재 진행 상태를 고정하고, 이후 작업을 체크리스트로 따라가며 갱신 |

> **사용법:** 작업이 끝나면 `[ ]` → `[x]`로 바꾸고, 맨 아래 [변경 로그](#변경-로그)에 날짜·한 줄을 추가한다.  
> 단계가 바뀌면 [현재 단계](#현재-단계)만 먼저 고친다.

---

## 한 줄 목표

노라무 글의 **차트 구조·눌림·무효화 논리**만 데이터화하고, 티커를 넣으면 **진입가·손절가·익절가**를 제안하는 PHP 보조 프로그램으로 완성한다.  
(레버·인버스·단타는 학습·출력에서 분리. 우선 1번 계좌 스윙 적용 → 이후 임의 티커·계좌 스위칭.)

---

## 현재 단계

**Phase 5 — 알림·분봉 보조** (진행) · ge70 라벨 보강 중

Phase 1 넘김 조건:

- [x] `entries.json`에서 학습용 / 제외용 / 미확정 심볼이 구분됨 (`learning_use`)
- [x] 1번 계좌 핵심 종목(MU, SNDK, 하이닉스, 삼성, 유진테크) OHLCV 캐시가 있음
- [x] `analyze_entries` + 케이스 A 대조 (`verify_case_a.php` → **PASS**, 절반 수치는 스윙 윈도우 갭 문서화)

---

## 현황 스냅샷 (2026-07-18)

### 완료된 것

| 영역 | 상태 | 위치 |
|------|------|------|
| 프로젝트·CLI 골격 | 완료 | `bin/`, `src/`, `composer.json` |
| 글 수집·병합 | 완료 | `bin/scrape_noramu.php`, `NoramuScraper` |
| 차단 시 브라우저 수입 | 완료 | `bin/import_json_posts.php`, `import_html_dir.php` |
| 진입 이벤트 저장 | 초기 샘플+스크랩 병합 | `data/entries.json` |
| 신호 추출(규칙) | 완료(1차) | `EntrySignalExtractor` |
| Yahoo OHLCV | 완료 | `YahooChartClient`, `bin/fetch_yahoo.php` |
| 피처·점수 | 완료(1차) | `FeatureEngine` (`pullback_long_score` 등) |
| 1번 계좌 변환 규칙 | 완료(1차) | `Account1Playbook`, `bin/score_symbol.php` |
| 학습 라벨 큐레이션 | 완료(1차) | `EntryCurator`, `bin/curate_entries.php` |
| 진입 구간·손절·익절 힌트 | 완료(1차) | `proposal.entry_zone` / `invalidation` / `target_hint` |
| 핵심 종목 일괄 점수 | 완료 | `bin/score_all.php` |
| 케이스 A 피처 대조 | 완료 | `bin/verify_case_a.php` → **PASS** (`flush_pre_peak`) |
| 구조 스윙 탐지 | 완료(1차) | 아래꼬리 슈팅 + 직전 피크 (`FeatureEngine`) |
| N일 백테스트 | 완료(1차) | `bin/backtest_entries.php` → `docs/backtest-latest.md` |
| 본주 full 시드 | 완료(1차) | MU 800 / 삼전 33만 / 하닉 190만 + scrape |
| 심볼 매핑 | 완료(1차) | `SymbolMap` (한글 별칭·Yahoo·차트 불가) |
| 제안 서비스 | 완료 | `ProposalService` (CLI/UI 공용) |
| 로컬 UI | 완료 | `index.php` + entries 목록 + TradingView |
| 알파 탭 | 완료(1차) | 노라무 / 디깅온유 / 합침 · `data/alpha/digingonyou_entries.json` |
| 디깅온유 기법 | 완료(1차) | `DigingonyouMethod` 투매저~+8% · playbook 문서 |
| 계좌 프로필 | 완료(1차) | `account1` / `custom` / `isa` (`AccountProfile`) |
| exit_reason 라벨 | 완료(1차) | `stop`/`target`/`observe` — curate 시 부여 |
| 주간 메모 | 완료(템플릿+W29) | `docs/weekly-memo-template.md`, `docs/weekly/` |
| 플레이북·재분석 메모 | 완료 | `docs/account1-playbook.md`, `retrospective-entries.md` |

### 아직 약한 점 / 미완

| 이슈 | 설명 |
|------|------|
| 라벨 ge70 | 엔진 스냅샷은 백테스트·hunt 매칭에서 제외. **노라무 원글** 26구간 미수집 (`ge70-label-hunt.md` + import 템플릿) |
| 백테스트 | 원글 full만 평가 (엔진 스냅샷 제외). 원글 ge70 보강 후 재평가 |
| UI | 임의 Yahoo 티커 + TV 임베드. 빠른링크 5종은 힌트일 뿐(학습/알림 배치만 CORE) |
| 알림 | 웹훅 지원(Discord/Slack). URL 미설정 시 JSON만 |

### 작업 흐름 (기준)

```
글 수집 → entries.json → 차트(OHLCV) → 피처 → 점수/진입 제안 → 1번 계좌 적용
                                         └→ (후기) 임의 티커 · 계좌 스위칭
```

---

## Phase 0 — 기반 (대부분 완료)

- [x] PHP 8.2+ CLI 프로젝트 구성
- [x] 에펨 노라무 글 스크래퍼 / HTML·JSON 수입
- [x] `entries.json` 저장소 (`EntryRepository`)
- [x] Yahoo 차트 캐시 (`data/ohlcv/`)
- [x] `FeatureEngine` + `Account1Playbook` + `score_symbol.php`
- [x] README·플레이북·초기 재분석 문서

---

## Phase 1 — 데이터 품질 · 라벨 정리

목표: 학습·점수에 쓸 수 있는 **깨끗한 진입 이벤트**만 남긴다.

### 1.1 수집·라벨

- [ ] 주 1회 이상 `php bin/scrape_noramu.php` (또는 차단 시 import)로 글 추가
- [ ] 수동 큐레이션: 명시 진입가·손절·목표가·구조 키워드가 있는 글 우선 보강 (`full` 확대)
- [x] `product_type` + `learning_use` 분리 플래그  
  - `full` / `structure_only` / `ignore` / `needs_review`  
  - 레버·가격 단위 이상 → `structure_only` + `exclude_price_label`  
  - SNDK 본주로 잡힌 10달러대 타점 → `SNDK_2X` 자동 재분류
- [x] `UNKNOWN` 이벤트 매핑 또는 `ignore` (`needs_review=0`, 2026-07-18)
- [x] `exit_reason` (`stop`/`target`/`observe`) — curate 시 자동 (stop=1, target=6 as of 2026-07-18)

### 1.2 차트·피처 검증

- [x] 핵심 심볼 캐시  
  - [x] `MU`  
  - [x] `SNDK`  
  - [x] `000660.KS`  
  - [x] `005930.KS`  
  - [x] `084370.KQ`
- [x] `analyze_entries.php --learning` + KST 타임존 보정 (미래 누수 방지)
- [x] 진입 시각 이전 봉만 사용 (`analyze_entries.php` posted_at 필터)
- [x] 케이스 A 대조 → PASS (구조·1485 무효화·07-08 종가). 엔진 절반≠수동 절반은 스윙 갭으로 문서화

### 1.3 주간 루틴 (운영)

- [x] `php bin/score_all.php` (CORE 5종목 일괄) — 2026-07-18 첫 실행
- [x] `php bin/score_symbol.php MU` — proposal 필드 확인
- [x] 주간 메모 템플릿 + `php bin/write_weekly_memo.php` → `docs/weekly/2026-W29.md`
- [x] `php bin/list_entries.php [full|structure_only|…]` 로 필터 확인

**완료 기준:** Phase 1 넘김 조건 충족 → Phase 2로 이동.

---

## Phase 2 — 규칙 기반 진입 제안 (1번 계좌)

목표: 티커 입력 시 **점수 + 진입 구간 + 손절(무효화) + 익절 후보**를 출력.

### 2.1 출력 스펙

- [x] CLI 출력 필드: `proposal`에 `entry_zone` / `invalidation` / `target_hint` 포함
- [x] `Account1Playbook`이 레버 심볼 입력 시 `blocked`
- [x] 절반 되돌림 ±4% 구간을 `entry_zone`으로 노출
- [x] `invalidation_level`(스윙 저점)을 손절 후보로 노출
- [x] 목표가 1차: `midpoint_half_to_swing_high`
- [x] 명시 목표가 학습값과 규칙 목표가 병기 (`LearnedLevels` → `target_learned` / `stop_learned`)

### 2.2 백테스트·라벨

- [x] 진입 후 N일(5/10/20) 수익률·손절/목표가 히트 (`EntryBacktester`, `backtest_entries.php`)
- [x] 점수 밴드별 표 (`docs/backtest-latest.md`) — 샘플 n=3 mid 밴드만
- [x] 임계값 재평가: **유지** (히스토리 ge70 26.7%, 라벨 ge70=0 → 문턱 내리지 않음). 문서 `threshold-review.md`
- [x] 레몬 `247960.KQ` → Yahoo 없음 처리 (`structure_only` / `SymbolMap` null)

### 2.3 1번 계좌 적용

- [x] CORE_SYMBOLS 배치 스코어 CLI (`bin/score_all.php`)
- [x] 주간 메모 템플릿 (`docs/weekly-memo-template.md` + `write_weekly_memo.php`)
- [x] 스윙 고저: `flush_pre_peak` (윅 슈팅 + 직전 피크). 케이스 A PASS
- [x] 실제 비중은 수동. 프로그램은 **제안만** (자동주문 없음)

**완료 기준:** `php bin/score_symbol.php MU` 한 번으로 진입·손절·익절 힌트가 JSON/텍스트로 나온다.

---

## Phase 3 — 보조 UI · UX

목표: 터미널만 말고, 빠르게 종목 넣어 보는 화면.

- [x] 로컬 PHP 페이지: 티커 입력 → 점수·구간·이유 표시
- [x] `index.php`를 실제 앱 엔트리로 교체 (`assets/app.css`)
- [x] entries 목록·최근 글 요약 뷰 (full/structure_only)
- [x] TradingView 링크 연결 (임베드 없음)

**완료 기준:** 브라우저에서 티커 넣고 1번 계좌용 제안이 보임. ✅

---

## Phase 4 — 임의 티커 · 계좌 스위칭 준비

목표: “1번 계좌 고정 종목”이 아니라 **원하는 티커**에 같은 논리를 적용.

- [x] 심볼 정규화 테이블 1차 (`SymbolMap` 한글·레버→본주·차트 불가)
- [x] 계좌 프로필 분리: `account1` / `custom` / `isa` (`AccountProfile` + `AccountPlaybook`)
- [x] 사용자 입력 → OHLCV → 피처 → 제안 파이프라인 단일화 (`ProposalService`)
- [x] ISA/연금: 문턱·비중 상향/축소 프로필 (`isa`)
- [x] 프로필별 주간 메모 (`write_weekly_memo.php --profile=`)

**완료 기준:** 임의의 Yahoo 심볼에 대해 동일 CLI/UI로 제안 가능. 1번 계좌 종목 리스트에 묶이지 않음. ✅ (`custom`)

---

## Phase 5 — 최종: 프로그램화 · (선택) 자동화

목표: README 최종목표 — 티커 입력 시 **진입가·손절가·익절가** 자동 제시.  
자동화는 그 다음.

### 5.1 완성도

- [x] 규칙 점수·임계값 권고 문서화 (`docs/threshold-review.md`, 히스토리 n=60)
- [x] full 라벨 확대 1차 (10건: +STX/ORCL/EWY)
- [ ] (선택) 데이터가 충분하면 로지스틱/단순 ML로 점수 보조 — 규칙은 유지
- [x] 1h 이상 거래량 보조 (`HourlyAssist`, UI/proposal에 메모)
- [x] 오염 라벨 자동 필터 (기존 curate + Yahoo 불가 종목)

### 5.2 자동화 (후순위 · 명시적 착수 후에만)

- [x] 알림 CLI (`bin/alert_watch.php`) — 콘솔/JSON
- [x] 메신저 웹훅 (`--webhook=` / `NORAMU_ALERT_WEBHOOK`, Discord/Slack)
- [x] ge70 구간 탐지·수집 가이드 (`find_ge70_windows`, `docs/ge70-label-hunt.md`)
- [x] 엔진 ge70 스냅샷 시드 (`seed_ge70_snapshots`) — 원글 대체 아님
- [x] hunt에서 엔진 스냅샷 ≠ 원글 매칭 분리 + 에펨 검색 링크
- [x] 브라우저 import 템플릿 (`prepare_ge70_import.php` → `data/raw/ge70_import/`)
- [ ] 노라무 원글로 ge70 구간 수동 보강 (hunt 문서 우선순위 · 템플릿 채우기)
- [x] 브로커 API: **비연동** 확정 (제안·알림만)
- [x] 계좌 금지 상품은 프로필 `blocked`로 하드블록 (연동 시에도 동일 규칙)

**완료 기준:** 티커만 넣으면 진입·손절·익절이 안정적으로 나오고, 자동화는 별도 게이트로 켠다.

---

## 가져올 것 / 버릴 것 (항상 유효)

| 노라무 | 이 프로젝트 |
|--------|-------------|
| 차트 구조 (슈팅 → 저점 상향 → 절반 눌림) | 사용 |
| 명시 진입·손절·목표가 | 데이터 → 점수·구간 |
| 레버 / 인버스 / 배수 | **폐기 안 함** → 본주 치환 (SOXS→MU 반전, 샌디2배→SNDK, 코루→삼전). 레버 호가≠본주가. 직접 매매만 비권고 |
| 양빵 | 애매하면 현금·관망 (양빵 자체는 비권고) |
| 초단기 -3% 단타 | 스윙 저점 이탈 시에만 비중 축소 |
| 애매하면 헤지 | 애매하면 현금·관망 |

---

## 자주 쓰는 명령

```bash
cd C:\Users\acdun\Desktop\dev\noramu

php bin/scrape_noramu.php --pages=5
php bin/import_json_posts.php data/raw/browser_posts.json
php bin/curate_entries.php                 # learning_use + exit_reason
php bin/fetch_yahoo.php MU 3mo 1d
php bin/analyze_entries.php --learning
php bin/verify_case_a.php
php bin/seed_spot_full.php
php bin/backtest_entries.php
php bin/score_symbol.php MU
php bin/score_symbol.php MU --profile=isa
php bin/score_symbol.php 하이닉스
php bin/score_all.php --profile=custom
php bin/score_history.php --weeks=12
php bin/find_ge70_windows.php --weeks=20
php bin/prepare_ge70_import.php --limit=8
php bin/import_doc_srls.php 10024780453   # 에펨 글번호 직접 import
php bin/seed_ge70_snapshots.php --limit=12
php bin/alert_watch.php --profile=account1
# php bin/alert_watch.php --webhook=https://discord.com/api/webhooks/...
php bin/write_weekly_memo.php
php bin/write_weekly_memo.php --profile=isa
php bin/list_entries.php full
# UI: Valet 또는 php -S 127.0.0.1:8765 -t .
```

---

## 다음으로 할 일 (이번 주 포커스)

> 매주 월요일에 이 섹션만 다시 쓴다. 끝나면 체크하고 로그에 남긴다.

1. [x] full 확대 + threshold-review — 문턱 유지
2. [x] 프로필별 주간 메모
3. [x] `alert_watch` + 1h + 웹훅
4. [x] ge70 구간 탐지 + 엔진 스냅샷 12건
5. [x] 규칙/학습 목표가 병기 + hunt·백테스트에서 엔진 스냅샷 분리 + import 템플릿
6. [x] 제안 문구 명확화 (`ProposalExplain` · UI/CLI · 플레이북 읽는 법) + 엔진 스냅샷 `structure_only`
7. [ ] `data/raw/ge70_import/` 템플릿에 **노라무 원글** 붙여넣기 (5월 하닉/MU, 6월 삼전 우선)
8. [ ] (선택) ML은 원글 ge70이 더 쌓인 뒤

---

## 변경 로그

| 날짜 | 변경 |
|------|------|
| 2026-07-18 | 로드맵 초안 작성. 현재 Phase 1. |
| 2026-07-18 | `EntryCurator`·`curate_entries`·`score_all` 추가. proposal 필드·CORE5 OHLCV. learning: full=5 / structure_only=26 / ignore=2 / needs_review=0. |
| 2026-07-18 | 케이스 A `verify_case_a` PASS. KST 타임존 수정. `exit_reason`·주간메모 W29. Phase 1 완료 → Phase 2. |
| 2026-07-18 | `flush_pre_peak`+아래꼬리 슈팅. `EntryBacktester`/`backtest_entries`. 케이스 A 재검증 PASS. |
| 2026-07-18 | `seed_spot_full`·`SymbolMap`·`ProposalService`·UI. full=7, backtest n=7. Phase 3 기본 완료. |
| 2026-07-18 | `AccountProfile`/`AccountPlaybook`. UI entries+TV. `--profile=` CLI. Phase 4 1차 완료. |
| 2026-07-18 | full=10. `score_history`·`threshold-review`(문턱 유지). `alert_watch`·`HourlyAssist`. 프로필 주간메모. |
| 2026-07-18 | `find_ge70_windows`·`seed_ge70_snapshots`·웹훅. full=22, backtest ge70=7. `ge70-label-hunt.md`. |
| 2026-07-18 | `LearnedLevels` 규칙/학습 목표가 병기. hunt·백테스트에서 엔진 스냅샷 제외. `prepare_ge70_import`. 브로커 비연동 확정. |
| 2026-07-18 | `ProposalExplain`·UI 라벨 명확화. 엔진 스냅샷 curate→structure_only. full=10. |
| 2026-07-18 | 전제 변경: 레버 폐기→`UnderlyingProxy` 본주 치환 (SOXS→MU 반전 등). |
| 2026-07-19 | 알파 탭: 노라무 / 디깅온유 / 합침. `ingest_digingonyou` 14 SRL · entries 분리 저장. |
| 2026-07-19 | 디깅온유 기법 1차: `DigingonyouMethod`(투매저 재진입) · `docs/digingonyou-playbook.md`. |
| 2026-07-19 | 디깅온유 SRL 추가 배치 ingest · «밟으면 매수»/2차지지 규칙 보강. |

---

## 관련 문서

- [README](../README.md) — 프로젝트 목적·디렉터리·주의
- [1번 계좌 플레이북](./account1-playbook.md) — 운영 규칙
- [진입 재분석](./retrospective-entries.md) — 초기 샘플·데이터화 메모
- [백테스트 latest](./backtest-latest.md) — full 라벨 N일 성과
- [임계값 리뷰](./threshold-review.md) — 히스토리 점수 분포·문턱 권고
- [ge70 라벨 수집](./ge70-label-hunt.md) — 고점수 구간·원글 수집 우선순위
- [보류 글](./parked-posts.md) — 지금은 안 넣고 나중에 쓸 글
